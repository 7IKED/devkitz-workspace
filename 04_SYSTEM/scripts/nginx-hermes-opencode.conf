# Hermes Dashboard
server {
    listen 443 ssl http2;
    server_name hermes.devkitz.eu;

    ssl_certificate /etc/letsencrypt/live/devkitz.eu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devkitz.eu/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:9119;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}

# Hermes API (OpenAI-kompatibel)
server {
    listen 443 ssl http2;
    server_name api-hermes.devkitz.eu;

    ssl_certificate /etc/letsencrypt/live/devkitz.eu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devkitz.eu/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8642;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# OpenCode Web
server {
    listen 443 ssl http2;
    server_name code.devkitz.eu;

    ssl_certificate /etc/letsencrypt/live/devkitz.eu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devkitz.eu/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:9900;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}

# Puter (fix: health check uses wrong domain)
server {
    listen 443 ssl http2;
    server_name puter.devkitz.eu;

    ssl_certificate /etc/letsencrypt/live/devkitz.eu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/devkitz.eu/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
